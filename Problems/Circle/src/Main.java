class Circle {

    double radius;

    // write methods here
    public double getLength(){
        this.radius = radius;
        double a = (radius + radius) * Math.PI * 2;


        return  a;
    }
    public double getArea(double radius){
        this.radius = radius;
        double b =radius * radius * Math.PI ;

        return b;
    }
}